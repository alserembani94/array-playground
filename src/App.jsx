/**
 * INFO: This project is to demo the usage of arrays,
 * and for that purpose, you can ignore this file, and
 * focus on the file that will be used for the tutorial.
 * Do not worry, you will learn about the routing soon
 * enough, but now, let's focus on array.
 * -Atif-
 */

import { RouterProvider } from "@tanstack/react-router";
import router from "./router";

function App() {
  return <RouterProvider router={router} />;
}

export default App;
